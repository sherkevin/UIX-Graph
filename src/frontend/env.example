# ============================================================
# 前端环境变量示例
# 使用方式（二选一）：
#   1. 复制为 .env（开发环境）或 .env.production（生产构建）并填写
#   2. 运行 scripts/switch_env.py [local|test|prod] 自动生成
# ============================================================

# 后端 API 根地址（不含尾部 /，不含 /api 路径）
# 开发环境（留空或不设置）：前端通过 Vite 代理 /api → http://localhost:8000
# 外网同源反代（推荐）：设置为空字符串或同源根，前端打包后直接用 /api 路径
# 外网跨域部署（备选）：填写后端公网地址，例如 https://api.example.com
VITE_API_BASE_URL=

# 是否启用 API 请求/响应详细日志（仅开发时开启）
VITE_ENABLE_DEBUG=false
